[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --cov=src/app
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=100
    --verbose
    --tb=short
asyncio_mode = auto
